# Invalid profile - not a Snowflake type
test_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: test_user
      password: test_password
      database: test_db

