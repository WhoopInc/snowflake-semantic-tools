# Valid profile with SSO/external browser authentication
test_project:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: test_account
      user: test_user
      authenticator: externalbrowser
      role: TEST_ROLE
      warehouse: TEST_WH
      database: TEST_DB
      schema: TEST_SCHEMA

