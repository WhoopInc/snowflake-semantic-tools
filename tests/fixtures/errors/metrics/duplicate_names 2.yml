snowflake_metrics:
  - name: total_revenue
    description: "First definition of total_revenue"
    expr: "SUM({{ column('orders', 'amount') }})"
    tables:
      - "{{ table('orders') }}"
    
  - name: total_revenue
    description: "Duplicate definition of total_revenue"
    expr: "SUM({{ column('transactions', 'value') }})"
    tables:
      - "{{ table('transactions') }}"
