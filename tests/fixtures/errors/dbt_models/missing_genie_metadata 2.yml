version: 2

models:
  - name: orders_without_sst
    description: "Orders table missing sst metadata"
    # Missing meta.sst.cortex_searchable: true
    columns:
      - name: id
        description: "Order ID"
      - name: amount
        description: "Order amount"
