snowflake_metrics:
  # These metrics have duplicate names - should fail validation
  - name: revenue
    expr: SUM(amount)
    entity_attribute: revenue
    table_name: "{{ table('transactions') }}"
    
  - name: revenue  # DUPLICATE NAME
    expr: SUM(revenue_amount)
    entity_attribute: total_revenue
    table_name: "{{ table('orders') }}"
