# Test fixtures for potential performance issues in relationships
# These should trigger warnings but not errors

snowflake_relationships:
  # Warning: Cross join potential (no conditions)
  - name: potential_cross_join
    description: "Warning: no relationship conditions specified"
    left_table: "{{ table('users') }}"
    right_table: "{{ table('products') }}"
    relationship_conditions: []
